export const selectTodos = (state) => state.todos.todos;

export const selectNextTodoId = (state) => state.todos.nextTodoId;

export const selectVisibilityFilter = (state) => state.todos.visibilityFilter;
